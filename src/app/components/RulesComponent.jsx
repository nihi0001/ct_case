// import {data} from "./rules";

export default function RulesComponent({params}) {
    // const {id} = params;
    // const rule = data.find(rule => rule.rulename === id);

  return (
    <div>
        <h1>rulename</h1>
        <p>description</p>
    </div>

  );
}

